# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# uv (dependency manager)
.venv/
venv/
# NOTE: uv.lock and .python-version are committed on purpose so `uv sync`
# reproduces the exact environment.

# Tooling caches
.coverage
htmlcov/
.ruff_cache/
.mypy_cache/
.pytest_cache/

# Environment / secrets
.env
.env.*

# Generated upload files (python -m qk80 --save)
*.tabml
*.abkt
*.abkg
*.anit
*.anim
*.anps
*.anpt

# graphify knowledge-graph outputs (generated; rebuild with /graphify)
graphify-out/

# Personal notes (not part of the project)
TODO.md

# Logs
*.log
TODO.md
graphify-out/

# Firmware reverse-engineering (raw binaries + tooling; don't commit)
firmware/
fw_analyze.py
uf2_extract.py
# ...but keep the write-up so others can reference it:
!FIRMWARE.md

# Editors / OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db
